{
  "name": "vaulttext-mcp",
  "version": "0.2.0",
  "description": "MCP server giving AI agents local text tools — stats, extract, find-replace, case (free), plus redact PII, sort/dedupe lines (Pro). Text never leaves the machine.",
  "type": "module",
  "main": "server.mjs",
  "bin": { "vaulttext-mcp": "server.mjs" },
  "files": ["server.mjs", "lib.mjs", "license.mjs", "README.md"],
  "scripts": { "test": "node smoke.mjs" },
  "keywords": ["mcp", "model-context-protocol", "mcp-server", "text", "regex", "privacy", "claude", "ai-agent", "ai-tools"],
  "license": "MIT",
  "engines": { "node": ">=18" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  }
}
